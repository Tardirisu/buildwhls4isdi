name: Build ARMv6 Wheels (Final Precise Build)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm

      - name: Build Specific Wheels in ARMv6 Container
        run: |
          # 清理宿主机缓存空间
          rm -rf ~/.cargo/registry
          mkdir -p ~/.cargo/registry
          mkdir -p ${{ github.workspace }}/wheelhouse
          
          docker run --rm \
            -v ${{ github.workspace }}:/io \
            -v ~/.cargo/registry:/root/.cargo/registry \
            -w /io \
            balenalib/rpi-debian:bookworm \
            /bin/bash -c "
              apt-get update && \
              apt-get install -y python3 python3-pip python3-dev \
                                 gcc g++ make curl cargo rustc pkg-config \
                                 libffi-dev libssl-dev cmake git && \
              export CARGO_REGISTRIES_CRATES_IO_PROTOCOL=sparse && \
              export CARGO_NET_GIT_FETCH_WITH_CLI=true && \
              python3 -m pip install --upgrade pip setuptools wheel maturin --break-system-packages || true && \
              python3 -m pip wheel -w /io/wheelhouse --no-deps \
                'qh3==1.0.1' \
                'pyimg4>=0.8.8' \
                'fastapi' \
                'wsproto' \
                'construct-typing' \
                'IPython'
            "

      - name: Upload Wheels as Artifact
        uses: actions/upload-artifact@v4
        with:
          name: raspberry-pi-armv6-precise-wheels
          path: wheelhouse/*.whl
